[package]
name = "styx-pset"
version = "0.1.0"
edition = "2021"
publish = false
description = "STYX v1 transaction builders: pure (state + intent) -> plan, sign, finalize"

[dependencies]
styx-core = { path = "../styx-core" }
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
